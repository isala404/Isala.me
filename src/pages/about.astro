---
import BaseLayout from '../layouts/BaseLayout.astro';
import { profileInfo as profile } from '../lib/profile';
---

<BaseLayout title="About | Isala Piyarisi" description={profile.longBio.slice(0, 160)}>
  <main class="max-w-content relative z-10 mx-auto px-6 py-16 md:py-24">
    <!-- Back Home -->
    <a href="/" class="text-summer-text-secondary arrow-link mb-8 inline-block text-sm">
      <span class="arrow">←</span><span class="text">back home</span>
    </a>

    <!-- Header -->
    <header class="mb-12">
      <h1 class="text-summer-text mb-4 text-3xl font-semibold tracking-tight md:text-4xl">
        The Long Version
      </h1>
    </header>

    <!-- Bio Section -->
    <section class="mb-12">
      <div class="prose prose-summer max-w-none">
        {
          profile.longBio
            .split('\n\n')
            .map((paragraph) => (
              <p class="text-summer-text mb-4 text-base leading-relaxed">{paragraph}</p>
            ))
        }
      </div>
    </section>

  </main>
</BaseLayout>
